name: Test Instagram Design (No Upload)

on:
  workflow_dispatch: # Manual trigger

jobs:
  test_design_only:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests Pillow tzdata

      - name: Run Generator (Cinematic Mode)
        working-directory: './cinema-scrapers'
        # FIXED: Updated filename to match V2 script
        run: python generate_post2.py
        env:
          TMDB_API_KEY: ${{ secrets.TMDB_API_KEY }}
          CLOUDINARY_URL: ${{ secrets.CLOUDINARY_URL }}
          STABILITY_KEY: ${{ secrets.STABILITY_KEY }}

      - name: Commit Generated Images for Inspection
        id: commit_push
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          
          # FIXED: Updated to match V2 output filenames
          git add 'cinema-scrapers/post_v2_image_*.png'
          
          # FIXED: Updated caption filename
          git add 'cinema-scrapers/post_v2_caption.txt'
          
          if git diff --staged --quiet; then
            echo "No design changes detected."
            echo "pushed=false" >> $GITHUB_OUTPUT
            exit 0
          fi
          
          git commit -m "DESIGN TEST: Generated Cinematic Backdrop slides (V2)"
          git pull origin main --rebase
          git push
          echo "pushed=true" >> $GITHUB_OUTPUT
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
