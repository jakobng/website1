# Phase 1 Scaffold Scenarios

These fixtures are for JP/IN/CN/MX expansion scaffolding and are intentionally
kept separate from `scenario_tests/scenarios` to avoid changing default test runs.

Use:

```powershell
python scenario_tests/run_phase1_scaffold.py
```

or run one file manually by loading it in a custom script.
